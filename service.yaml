apiVersion: v1
kind: Service
metadata:
  name: road-frontend
spec:
  selector:
    app: road-frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80           
      targetPort: 80    
      nodePort: 30010
